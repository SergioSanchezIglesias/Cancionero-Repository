{
  "name": "@cancionero/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && rm -rf dist/db/migraciones && cp -R src/db/migraciones dist/db/",
    "migrar": "npm run build && node dist/migrar.js",
    "start": "node dist/servidor.js",
    "dev": "npm run build && node dist/servidor.js",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.test.json",
    "pretest": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@cancionero/chords": "^0.1.0",
    "better-sqlite3": "^13.0.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/node": "^22.15.0",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}

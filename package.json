{
  "name": "cancionero",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run build -w packages/chords && npm run test --workspaces --if-present",
    "build": "npm run build -w packages/chords && npm run build -w packages/api"
  }
}
